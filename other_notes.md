At some point consider refactoring remaining references to case1/case2/case3 instances ...
At some point take a good look at the config file and consider which are necessary to keep, which should be renamed or merged, and which we can get rid of. 
When running with kitti: look at the is_distort setting and why some points in kitti had curvature values of 666 - where is this value being set? Is it a rosnode that is marking these points as distorted this way? Why is there 0 mention of this in the paper, supplementary material, or original code? Why is it only relevant for kitti according to the original code? How can we reincorporate handling distorted points in a more robust way for every dataset? Are there existing methods to handle and reject distorted points that we can leverage in our algorithm in a more sensible way (potentially as a pre-processing step)?
Look into better way to log debugging prints of different functions/files/types/levels ... 