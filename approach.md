Project description: The goal is to refactor the existing m-detector code (a ROS project in C++ to perform moving event detection in LiDAR data) to work without depending on ROS and to provide a convenient interface through python for offline processing. The idea is that the existing algorithm is kept in C++, but python libraries like the nuscenes devkit can be used to prepare the data. The final version of the project should be able to support multiple datasets like nuscenes, truckscenes, waymo, and kitti in a way that the C++ code itself does not care which dataset the data comes from. In theory, data preparation should be performed in python in a way that the data fed into the algorithm is consistent no matter the source dataset, though the configuration file can be tuned to each dataset. 

Note that the original m-detector code depended on odometry generated by fast-lio2 (another ROS dependent package) using the given LiDAR and raw IMU data. However, nuscenes provides egopose information, which should remove the need for fast-lio2, which would be a significant reduction in the refactoring effort. When the project gets to the point that this can be tested, this should be investigated more thoroughly, i.e. by comparing the odometry in the rosbags generated with fast-lio2 with the egopose data provided directly by nuscenes, and whether any differences between the two can/must be accounted for in the refactored code or not. 

Now follows the initial overview of the phases of the project. 

Phase 1: Isolate and Test DynObjFilter without ROS

    Setup & Baseline:
        Create a new branch or copy of the project for refactoring.
        Ensure the original ROS version builds and runs correctly (this is your baseline).
        Set up a new CMake project (CMakeLists.txt) that does not use catkin or find ROS packages. It should find Eigen, PCL (if needed by DynObjFilter itself, though it seems less reliant than DynObjCluster), and C++17 (for std::execution::par).

    Remove ROS Includes & Basic Types (within DynObjFilter files):
        Go through DynObjFilter.h, DynObjFilter.cpp, and any directly related helper files (like RingBuffer, DepthMap, point_soph).
        Remove #include <ros/ros.h> and other ROS headers.
        Replace ros::Time with std::chrono::time_point<std::chrono::high_resolution_clock> or double for timestamps. Be consistent.
        Replace ROS logging (ROS_INFO, etc.) with std::cout or a simple logging library like spdlog.
        Goal: Get DynObjFilter.cpp and its direct, non-ROS dependencies to compile using your new non-ROS CMake setup. It won't link or run yet.

    Create a Standalone main Entry Point:
        Create a new main.cpp file (or similar).
        Include DynObjFilter.h.
        Instantiate DynObjFilter: std::shared_ptr<DynObjFilter> dyn_filter(new DynObjFilter());.
        Goal: Have a basic executable structure that includes the filter class.

    Replace ROS Parameter Loading:
        The DynObjFilter::init(ros::NodeHandle& nh) function uses nh.param<...>().
        Modify DynObjFilter::init to accept parameters differently, e.g., via a struct, a configuration file path, or individual arguments.
        In your main.cpp, implement a simple configuration loader (e.g., read a basic text file, JSON, or YAML using libraries like nlohmann/json or yaml-cpp) to load the parameters needed by DynObjFilter.
        Call the modified dyn_filter->init(...) with the loaded parameters.
        Goal: Initialize DynObjFilter without ROS NodeHandle.

    Replace ROS Callbacks with Direct Calls (Mock Data):
        The original code uses PointsCallback and OdomCallback to receive data and then calls DynObjFilter::filter via a timer.
        In your main.cpp, create a simple loop.
        Inside the loop, create mock input data:
            A pcl::PointCloud<PointType>::Ptr (e.g., feats_undistort). Populate it with a few dummy points.
            An Eigen::Matrix3d rot_end (e.g., identity).
            An Eigen::Vector3d pos_end (e.g., zero).
            A double scan_end_time (e.g., incrementing).
        Directly call dyn_filter->filter(feats_undistort, rot_end, pos_end, scan_end_time);.
        Goal: Run the core filtering logic on demand with controlled inputs, completely independent of ROS topics/callbacks. Test if it runs without crashing.

    Replace ROS Output with Simple Verification:
        The DynObjFilter::filter function populates member point clouds (laserCloudDynObj_world, laserCloudSteadObj). The publish_dyn function (called later) used ROS publishers.
        Remove the publish_dyn call for now, or modify it to not use ROS publishers.
        After calling dyn_filter->filter, access the resulting point clouds (dyn_filter->laserCloudDynObj_world, dyn_filter->laserCloudSteadObj).
        Add simple checks: Print the number of dynamic/static points found. Optionally, save these clouds to PCD files using PCL for visual inspection (pcl::io::savePCDFileASCII).
        Goal: Verify that the filter produces some output based on the mock input, without using ROS publishers.

Phase 2: Integrate nuScenes Data Loading

    nuScenes Data Input Pipeline:
        Write a Python script or set of scripts using nuscenes-devkit to iterate through scenes/sweeps, load LiDAR paths and ego poses, and use byind for data communication. 
        Feed the loaded point cloud and ego_pose (as rot_end, pos_end, scan_end_time) into dyn_filter->filter in your loop.
        Goal: Run the standalone filter on actual nuScenes data. Test with a single lidar sweep scene first, then a complete scenes, then the complete dataset.

    Handling ego_pose:
        The DynObjFilter::filter function signature already takes const M3D & rot_end, const V3D & pos_end, const double & scan_end_time.
        This maps directly to the data you get from ego_pose.json for the corresponding LiDAR sample_data timestamp. No complex conversion is needed here compared to integrating raw IMU data.
        Goal: Confirm that the ego_pose data is correctly extracted and passed to the filter function.

Phase 3: Refinements and Future Work Prep

    Testing and Debugging: Thoroughly test the output against the baseline ROS version if possible, or by visual inspection of saved PCD files. Debug any discrepancies.

    Offline Processing (Past/Future):
        The current DynObjFilter already uses a RingBuffer (buffer) and DepthMap history (depth_map_list). This structure is inherently suitable for offline processing.
        Since you're loading data sequentially offline, you can easily load multiple future and past scans/poses into memory if needed.
        Modifications for past/future tracking would involve:
            Potentially increasing the size/duration managed by RingBuffer and depth_map_list.
            Adjusting the logic within Case1/2/3 or adding new logic to explicitly query future/past depth maps or point states.
        The refactored structure allows these algorithmic changes without fighting ROS infrastructure.

    Re-integrate Clustering/Frame-Out processing (Later): Once DynObjFilter is stable standalone, you can apply the same refactoring steps (remove ROS, replace params, replace callbacks, replace publishers) to DynObjCluster and call it after DynObjFilter::filter, passing the necessary point clouds and tags, or implement a new frame-out processing step to perform filtering on the point-based detection algorithm (point-out) to enhance temporal and spatial consistency. This could potentially use both past and future data as the algorithm is intended to run offline on existing LiDAR data. 

    Investigate processing other datasets such as truckscenes, waymo, kitti, etc. in a similar way. Ideally, the existing setup is built in such a way that expanding to other datasets is a matter of figuring out the right way to present the data from each set to the same pybind interface without requiring changes to the underlying C++ code. 

    Investigate other potential optimizations or removal of dependencies that came up while refactoring the code. Some may have been left for future consideration as the primary goal is to remove the ROS dependency and to make it work so that the results can be compared - additional optimizations can break the code or change the results, which will make it very hard to retrace our steps to a working version. 


Specific notes about refactoring the code:

(parts between xxx are done)
xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx

    File 1: DynObjFilterConfig.h/.cpp (NEW)
        Purpose: Handle configuration loading and storage.
        Contents:
            Define a struct DynObjFilterParams (or similar) to hold all the parameters currently loaded via nh.param.
            Function loadParamsFromYaml(const std::string& filename): Uses yaml-cpp to load parameters into the struct.
            (Maybe) Keep constants like PI_MATH, MAX_1D, MAX_2D_N here or in a separate constants.h.

    File 2: DynObjDataTypes.h (NEW)
        Purpose: Define core data structures, ROS-free.
        Contents:
            Definition of point_soph struct/class (ensure no ROS types inside).
            Definition of DepthMap struct/class (ensure no ROS types inside, replace project_R/project_T if they are ROS TF types, use Eigen).
            Enum dyn_obj_flg.
            Typedefs like DepthMap2D.
            Includes for Eigen, PCL, <vector>, <deque>.


    File 3: PointCloudUtils.h/.cpp (NEW)
        Purpose: General point cloud and geometric utilities.
        Contents (Functions made static or part of a namespace):
            SphericalProjection(...) (Input: point_soph, map index, Eigen transform; Output: spherical coords in point_soph)
            InvalidPointCheck(...) (Input: Eigen V3D, intensity, const DynObjFilterParams& params; Output: bool)
			SelfPointCheck(...) (Input: Eigen V3D, const DynObjFilterParams& params; Output: bool). Good starting point after param refactor.
            CheckVerFoV(...) (Input: point_soph, const DepthMap&, const DynObjFilterParams& params; Output: bool). Depends on DepthMap.
            CheckNeighbor(...) (Input: point_soph, const DepthMap&, float& max_d, float& min_d, const DynObjFilterParams& params). Depends on DepthMap.
            DepthInterpolationStatic(...) (Input: point_soph&, map index, const DepthMap2D&, const DynObjFilterParams& params; Output: float).
            DepthInterpolationAll(...) (Input: point_soph&, map index, const DepthMap2D&, const DynObjFilterParams& params; Output: float). 
            
    Note that the DepthInterpolation functions have been refactored to findInterpolationNeighbors, computeBarycentricDepth, and interpolateDepth. 

xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx

    INTERMEDIATE STEP: Dataloading and pybind interface -> possibly testing fundamental code above with real data - WIP

    File 4: DetectionLogic.h/.cpp (NEW)
        Purpose: The core dynamic object detection algorithms (Case 1, 2, 3).
        Contents (Functions made static or part of a namespace):
            Case1(...) (Input: point_soph&, const std::deque<DepthMap::Ptr>& depth_maps, const DynObjFilterParams& params; Output: bool). Depends on helpers.
            Case1Enter(...) (Input: const point_soph&, const DepthMap&, const DynObjFilterParams& params; Output: bool). Depends on CheckVerFoV, CheckNeighbor.
            Case1FalseRejection(...) -> Calls Case1MapConsistencyCheck.
            Case1MapConsistencyCheck(...) (Input: point_soph&, const DepthMap&, const DynObjFilterParams& params, bool interp; Output: bool). Depends on DepthInterpolationStatic.
            Case2(...) (Input: point_soph&, const std::deque<DepthMap::Ptr>& depth_maps, const DynObjFilterParams& params; Output: bool). Complex state/history tracking.
            Case2Enter(...)
            Case2MapConsistencyCheck(...) (Depends on DepthInterpolationAll)
            Case2SearchPointOccludingP(...)
            Case2IsOccluded(...)
            Case2DepthConsistencyCheck(...)
            Case2VelCheck(...)
            Case3(...) (Input: point_soph&, const std::deque<DepthMap::Ptr>& depth_maps, const DynObjFilterParams& params; Output: bool). Complex state/history tracking.
            Case3Enter(...)
            Case3MapConsistencyCheck(...) (Depends on DepthInterpolationAll)
            Case3SearchPointOccludedbyP(...)
            Case3IsOccluding(...)
            Case3DepthConsistencyCheck(...)
            Case3VelCheck(...)

    File 5: DynObjFilter.h/.cpp (REFACTORED Original)
        Purpose: The main class managing state and orchestrating the process.
        Contents:
            DynObjFilter class definition.
            Member variables: DynObjFilterParams params;, Buffer buffer;, std::deque<DepthMap::Ptr> depth_map_list;, std::vector<point_soph*> point_soph_pointers;, Cluster cluster; (if kept/refactored), timing vars.
            init(const std::string& config_path): Calls loadParamsFromYaml, initializes buffer, pointers, Cluster object based on loaded params. (Replaces ROS init).
            filter(PointCloudXYZI::Ptr feats_undistort, const Eigen::Matrix3d& rot_end, const Eigen::Vector3d& pos_end, const double& scan_end_time):
                Main loop calling utility functions (InvalidPointCheck, SelfPointCheck from PointCloudUtils) and core logic (Case1, Case2, Case3 from DetectionLogic), passing params struct where needed.
                Handles Cluster interaction (needs refactoring if Cluster is kept).
                Calls Points2Buffer, Buffer2DepthMap.
                Returns filtered data (e.g., separate PCL clouds for dynamic/static points) instead of relying on publish_dyn.
            Points2Buffer(...): (Input: vector<point_soph*>& points). Manages buffer.
            Buffer2DepthMap(...): (Input: double cur_time). Manages depth_map_list. Calls SphericalProjection.
            set_path(...): (Can stay).
            (Remove publish_dyn).

